import 'package:flutter/material.dart';

// Questa è la classe TextHeadline che estende StatelessWidget.
// Questo componente viene utilizzato per visualizzare il titolo nella AppBar.
class TextHeadline extends StatelessWidget {
  const TextHeadline({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Stateless");
  }
}
